// Write a function addr that takes two numbers as arguments and returns the sum of these two numbers when executed.

function sum(a, b) {
  return a + b;
}

console.log(sum(2, 7));

// Write a function customGreeting that takes two strings and returns a greeting. The first argument should be something like Hello or Hey and the second is the name of a person.

function customGreeting(greeting, name) {
    return `${greeting} ${name}`
}

console.log(customGreeting("Hello", "John"));

